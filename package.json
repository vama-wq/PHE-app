{
  "name": "phe-app",
  "version": "1.0.0",
  "description": "Peena Heat Elements Management System",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "npm install --prefix server && NODE_ENV=development npm install --prefix client && cd client && npx vite build",
    "start": "node server/src/index.js",
    "setup": "cd server && npm run setup"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
